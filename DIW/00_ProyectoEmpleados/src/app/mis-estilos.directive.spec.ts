import { MisEstilosDirective } from './mis-estilos.directive';

describe('MisEstilosDirective', () => {
  it('should create an instance', () => {
    const directive = new MisEstilosDirective();
    expect(directive).toBeTruthy();
  });
});
