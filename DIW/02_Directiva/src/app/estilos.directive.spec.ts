import { EstilosDirective } from './estilos.directive';

describe('EstilosDirective', () => {
  it('should create an instance', () => {
    const directive = new EstilosDirective();
    expect(directive).toBeTruthy();
  });
});
